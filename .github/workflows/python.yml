name: Test python.sh

on: push

jobs:
  test-on-macos-ventura-x86_64:
    runs-on: macos-13
    steps:
      # Checkout this repo
      - name: Checkout
        uses: actions/checkout@v2
      # Run a script using zsh
      - name: Run script
        run: |
          ./macos/install/python.sh
        shell: zsh {0}
  
  test-on-macos-sonoma-arm64:
    runs-on: macos-14
    steps:
      # Checkout this repo
      - name: Checkout
        uses: actions/checkout@v2
      # Run a script using zsh
      - name: Run script
        run: |
          ./macos/install/python.sh
        shell: zsh {0}